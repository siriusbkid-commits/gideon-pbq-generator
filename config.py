import os

# Simulator always runs in local (offline) mode
SIMULATOR_MODE = "local"

# Local Ollama model
LOCAL_MODEL = "mistral:7b-instruct-q4_K_M"

# PBQ generation mode (Phase 1 = small, offline-friendly)
PBQ_MODE = "small"

